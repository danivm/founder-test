/**
 * @param {Object} props
 * @param {RequirementEntity[]} props.requirements
 */

const RequirementListValueObject = ({requirements}) =>
  Object.freeze({
    requirements
  })

export default RequirementListValueObject
